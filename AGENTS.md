# Sourcetree 桌面客户端开发约定

本文件是本仓库内进行功能开发、界面调整、重构和代码审查时的项目级约定。它适用于
`lib/`、`test/`、`integration_test/`、`macos/` 以及与产品行为相关的文档变更。

## 文档同步

涉及用户可见行为、交互、数据边界或维护流程的代码改动，完成实现后必须同步更新文档：

- `README.md`：在“当前状态”或相关能力说明中记录实际行为、前置条件和已知限制。
- `docs/IMPLEMENTATION_PLAN.md`：作为本项目唯一的路线图和任务清单，更新对应条目的状态；
  已交付能力归入已完成或当前版本的范围，不保留与实现不符的计划描述。
- `RELEASE_NOTES.md`：面向未发布版本记录用户能感知的新增、修复、限制和行为变化；若文件
  尚不存在，首次需要发布说明的改动应先创建该文件，并从 `## Unreleased` 开始记录。

文档描述当前实现，不记录临时调试过程、未实现的设想或无法验证的承诺。仅影响测试、内部
重构或不会改变用户可见行为的改动，可以不新增发布说明，但应在代码审查说明中写明理由。

## 验证

提交前按改动范围运行并通过：

```sh
dart format <changed-dart-files>
flutter analyze
flutter test <related-tests>
```

涉及 macOS 窗口、分栏布局、悬停菜单、快捷键、窗口关闭、Flutter Engine 生命周期或异步
状态刷新的改动，还应补充或运行覆盖窗口边界、生命周期、并发打开/关闭和异步回调失效的测试。
涉及平台通道、AskPass、真实 Git 子进程或跨窗口协作时，按需运行：

```sh
flutter test integration_test/macos_core_workflow_test.dart -d macos
flutter build macos --debug
```

测试不得依赖开发者本机私有仓库、凭据、固定绝对路径或不可重复的网络状态。验证失败时，
在交付说明中明确失败命令、原因和未覆盖的边界。

## Sourcetree 一致性

所有新增或修改的功能与界面都应无限接近 Sourcetree 的公开产品体验，同时保持本项目独立
实现的边界：

- 优先复用 Sourcetree 的信息层级和工作流语义：仓库/引用导航、提交 Graph、提交详情、
  Changes/Diff、暂存区，以及 Fetch、Pull、Push、Commit、Branch 等高频操作的布局和节奏。
- 保持桌面 Git 客户端的高信息密度三栏结构、清晰的当前分支和工作区状态、可追溯的操作反馈，
  并让窗口缩放、分栏调整、键盘操作和上下文菜单在不同尺寸下仍然可用。
- 沿用 Sourcetree 风格的交互预期：危险操作先解释影响再确认；不可安全执行的操作根据仓库
  状态、upstream、ahead/behind、冲突和正在运行的任务明确禁用；Fetch/Pull/Push 等后台操作
  显示进度、认证等待、取消和错误恢复路径。
- 视觉上使用紧凑但可读的间距、明确的层级、克制的中性色表面、Git 状态色和单一强调色；
  同时完整支持浅色/深色主题、系统字体、文字缩放和键盘焦点。不得引入 Codex 专属的文案、
  图标、导航结构或交互暗示。
- 继续遵守本项目的 macOS 单进程多窗口模型：首页与单仓库工作区各自独立窗口，同一路径不
  重复创建窗口，关闭窗口释放自身资源而不影响其他仓库。
- Git CLI 和仓库状态是唯一事实来源；界面显示、缓存、进度和本地偏好都不能伪造或覆盖 Git
  结果。优先保证数据不损坏、状态正确、操作可恢复，再考虑性能和视觉相似度。
- 不复制 Sourcetree 的源码、二进制、商标、Logo、品牌素材、专有文案、私有接口或隐藏行为。
  对齐仅限于公开可观察的功能、信息架构和交互语义；本项目必须保留自己的实现、名称和资源。
- 如果本地业务边界、Git 安全策略或平台限制导致行为不同，保留 Sourcetree 的视觉语言和
  信息层级，并在 `README.md`、实施规划和发布说明中明确记录差异及其原因。

## 代码与状态边界

- UI 不直接拼接或执行 Git 命令；统一通过应用层和 `GitRunner`，并在写操作后重新读取仓库状态。
- 所有异步任务、窗口回调和 Flutter Engine 生命周期都必须具备取消、失效和错误路径，不能在
  Widget 已销毁后更新状态。
- 修改具名 Dart 方法时，保留或补充声明处的中英双语 DartDoc，说明真实职责、输入输出和
  生命周期边界；同时更新相关测试。
- 不擅自执行 `reset --hard`、`clean`、强制推送、删除未合并分支或删除未知 `index.lock` 等
  不可逆操作；若产品确实需要，必须有明确的用户确认和可见影响说明。
