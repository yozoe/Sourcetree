# Release Notes

## Unreleased

- 修复历史提交右键菜单被标签功能替换的问题：现已恢复检出、合并、从当前提交创建分支、标签与复制 SHA-1。推送修订版本、变基、重置、提交回滚、创建补丁和遴选尚未实现，菜单会明确禁用这些操作。
- 修复附注标签留空说明时无法安全创建的问题；标签创建失败会显示 Git 的实际原因，若本地创建成功但远端推送失败，会保留本地标签并明确说明部分成功结果。
- 修复历史“合并…”忽略被选提交的问题；现在会确认并合并该提交。非提交对象标签不再被错误地作为提交详情加载；远端标签删除成功但本地删除失败时会明确显示部分完成状态。
- 左侧“工作区”新增“历史”入口。“文件状态”仍提供完整的工作区文件与提交入口；在“历史”中点击 `Uncommitted changes` 则保留提交图和历史分栏，仅选中该虚拟行并在下方显示工作区改动，不再跳转到“文件状态”。
- 新增历史提交的 Tag 工作流：右键提交选择“标签…”后可创建轻量或带说明的附注标签，默认指向该提交，也可选择已加载的其他提交；可选只推送该标签到一个已配置远端。左侧“标签”现在显示 Git 读取到的本地标签并可定位目标提交；删除页支持删除本地标签，或在明确确认后同时删除指定远端同名标签。所有写操作结束后重新读取 Git 引用，远端推送失败不会掩盖已成功创建的本地标签。
- 左侧远端导航现在按已配置远端分组，展开后显示 Git 返回的 `HEAD` 符号引用和远端分支；右键远端名称可从该远端获取，并打开预选该远端的拉取、推送配置。“移除远端”会明确说明影响并确认，只移除本地 Git 配置和跟踪引用，绝不删除远端仓库或远端分支。
- 选中左侧“文件状态”后，引用导航右侧现在完整显示工作区文件、Diff 与提交信息入口，并隐藏常规历史和提交详情分栏；底部入口继续使用既有提交面板和 Git 写入流程。
- 新增完整的贮藏工作流：工具栏“贮藏”和左侧“已贮藏”入口（固定显示在远端之后）以紧凑创建弹框保存当前改动，可填写说明并保留暂存区；入口支持重复打开，且在没有可保存的已跟踪改动、存在冲突或 Git 操作进行中时明确禁用。左侧“已贮藏”会固定展开真实 Git 贮藏列表，每条记录显示分支和说明；点击会隐藏常规历史与详情分栏，并在引用导航右侧完整预览其中的文件和 Diff，右键菜单可列出、恢复并保留、恢复并弹出，以及明确确认后删除。默认不包含未跟踪文件。恢复仅在干净工作区执行，使用 `--index` 还原暂存区，并在冲突时保留贮藏和刷新冲突状态；未跟踪的嵌套 Git 仓库需在其自身仓库中单独处理。
- 抓取操作改为配置面板：默认抓取全部远端，可选清理失效跟踪分支及抓取所有标签；真实 Git 操作仍支持取消并在结束后刷新引用状态。
- 推送操作升级为多分支配置面板：可选择远端、勾选分支、手动输入或选择目标远程分支、设置跟踪关系及推送所有标签；推送始终不使用 force，并会在结束后刷新 Git 状态。
- 新增带动画的分支管理面板：支持从当前 HEAD 或指定历史提交创建本地分支、选择是否立即检出；删除页可多选本地/远端分支、显示类型列，并在明确确认后安全删除、强制删除未合并本地分支或请求删除远端分支。
- 修复复杂游离 HEAD 历史使用旧 Graph 算法导致节点、连线和共同祖先汇合不完整的问题；当前 HEAD 谱系现在稳定占用主蓝色车道，历史搜索也继续复用完整历史计算出的 Graph 颜色与拓扑。
- 修复嵌套合并历史中过早压缩车道、遗漏分支节点和连接线的问题；并行分支现在保持独立车道直到共同祖先行再汇合，节点、连线、提交标签和左侧图标统一使用实际车道颜色，多个侧分支可显示红、橙、绿、紫、青等颜色。
- 修复 detached HEAD 下无法切回本地分支的问题：普通未提交或未跟踪改动现在会随安全的 `git switch` 保留；存在未解决冲突时仍会禁用切换，Git 拒绝覆盖风险时保留原状态。
- 历史提交列表移除行间分割线，并将每行高度从 20px 微调至 24px，让提交图、引用和提交说明更易于连续扫读。
- 恢复历史列表的图表、描述、提交、作者、日期表头；表头与提交行高度独立，仍保持 24px 行高和无行间分割线。
- 修复游离 HEAD 位于分支共同基点时的 Graph 车道分配：最左工作区车道现在连续贯穿到 HEAD 基点，`main` 和 `codex/conflict-demo` 仅从各自提交开始使用右侧独立车道并回连基点，不再出现断线或提前绘制的侧分支。
- 蓝色主分支与红色侧分支现在保持各自车道直到橙色共同父节点所在行，再于父节点处汇入，不再在各自末端提交行提前向左转向。
- 提交图逐车道核对上一行的真实连接，蓝色和红色分支首节点上方不再绘制悬空的多余竖线，同时保留通往共同父节点的连续线段。
- 主分支、其他本地分支和 HEAD/远端标签现在分别复用蓝、红、橙 Graph 色；主分支领先 upstream 时在提交行显示“超前 N 个版本”。
- 历史引用标签现在会在游离 HEAD 时继续把可推送的主本地分支（如 `main`）显示为蓝色，并用红色区分其他本地分支、橙色区分远端与 HEAD；单个长分支名会优先完整显示，`codex/conflict-demo` 不再被固定 100px 宽度截断。
- 修复移除历史范围工具条后列表被错误锁定为“当前分支”的回归；无工具条模式现在始终展示 Git 已加载的全部分支提交与完整拓扑。
- 历史视图改为 Sourcetree 风格的紧凑提交列表：移除常驻显示工具条，保留列表表头，提交图与背景随浅色/深色主题切换；列宽拖拽仍可在历史内容中使用。
- 提交引用标签现在按当前本地分支、其他本地分支、远端分支和游离 HEAD 使用不同的图标与边框，便于快速识别引用类型。
- 左侧“分支”导航中的斜杠分隔分支现在会按可展开目录显示，例如 `codex/conflict-demo` 会显示为 `codex` 下的 `conflict-demo`；选择、切换与右键操作保持不变。
- 推送工具栏现在显示当前分支领先 upstream 的提交数；有远端推送目标时保持可点击，没有领先提交时执行无改动检查；游离 HEAD 时可使用已配置远端的本地分支完成推送。
- 修复游离 HEAD 推送目标在界面和执行层选择不一致、异常核验比较错误 HEAD，以及无工作区改动时预留灰色车道悬空的问题；附着 HEAD、多个同源侧分支、窄窗口长标签和提交详情引用颜色均补齐边界处理。
- 新增 Changes 面板的文件列表宽度调整，以及已暂存/未暂存文件区域高度调整。
- 在窗口空间不足时自动按最小边界收缩列和面板，并补充对应的 Widget 交互测试。
- 未跟踪目录现在展开显示其中的文件；Changes 支持 Command 多选及批量暂存/取消暂存。
- 新增文件右键菜单中的路径复制、Finder 定位和未跟踪文件删除（确认后执行，已跟踪改动受保护）。
