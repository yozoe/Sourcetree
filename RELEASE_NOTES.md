# Release Notes

## Unreleased

- 修复复杂游离 HEAD 历史使用旧 Graph 算法导致节点、连线和共同祖先汇合不完整的问题；当前 HEAD 谱系现在稳定占用主蓝色车道，历史搜索也继续复用完整历史计算出的 Graph 颜色与拓扑。
- 修复嵌套合并历史中过早压缩车道、遗漏分支节点和连接线的问题；并行分支现在保持独立车道直到共同祖先行再汇合，节点、连线、提交标签和左侧图标统一使用实际车道颜色，多个侧分支可显示红、橙、绿、紫、青等颜色。
- 修复 detached HEAD 下无法切回本地分支的问题：普通未提交或未跟踪改动现在会随安全的 `git switch` 保留；存在未解决冲突时仍会禁用切换，Git 拒绝覆盖风险时保留原状态。
- 历史提交列表移除行间分割线，并将每行高度从 20px 微调至 24px，让提交图、引用和提交说明更易于连续扫读。
- 修复游离 HEAD 位于分支共同基点时的 Graph 车道分配：最左工作区车道现在连续贯穿到 HEAD 基点，`main` 和 `codex/conflict-demo` 仅从各自提交开始使用右侧独立车道并回连基点，不再出现断线或提前绘制的侧分支。
- 蓝色主分支与红色侧分支现在保持各自车道直到橙色共同父节点所在行，再于父节点处汇入，不再在各自末端提交行提前向左转向。
- 提交图逐车道核对上一行的真实连接，蓝色和红色分支首节点上方不再绘制悬空的多余竖线，同时保留通往共同父节点的连续线段。
- 主分支、其他本地分支和 HEAD/远端标签现在分别复用蓝、红、橙 Graph 色；主分支领先 upstream 时在提交行显示“超前 N 个版本”。
- 历史引用标签现在会在游离 HEAD 时继续把可推送的主本地分支（如 `main`）显示为蓝色，并用红色区分其他本地分支、橙色区分远端与 HEAD；单个长分支名会优先完整显示，`codex/conflict-demo` 不再被固定 100px 宽度截断。
- 修复移除历史范围工具条后列表被错误锁定为“当前分支”的回归；无工具条模式现在始终展示 Git 已加载的全部分支提交与完整拓扑。
- 历史视图改为 Sourcetree 风格的紧凑提交列表：移除常驻显示工具条和列表表头，提交行缩至 20px，提交图与背景随浅色/深色主题切换；列宽拖拽仍可在历史内容中使用。
- 提交引用标签现在按当前本地分支、其他本地分支、远端分支和游离 HEAD 使用不同的图标与边框，便于快速识别引用类型。
- 左侧“分支”导航中的斜杠分隔分支现在会按可展开目录显示，例如 `codex/conflict-demo` 会显示为 `codex` 下的 `conflict-demo`；选择、切换与右键操作保持不变。
- 推送工具栏现在显示当前分支领先 upstream 的提交数；有远端推送目标时保持可点击，没有领先提交时执行无改动检查；游离 HEAD 时可使用已配置远端的本地分支完成推送。
- 修复游离 HEAD 推送目标在界面和执行层选择不一致、异常核验比较错误 HEAD，以及无工作区改动时预留灰色车道悬空的问题；附着 HEAD、多个同源侧分支、窄窗口长标签和提交详情引用颜色均补齐边界处理。
- 新增 Changes 面板的文件列表宽度调整，以及已暂存/未暂存文件区域高度调整。
- 在窗口空间不足时自动按最小边界收缩列和面板，并补充对应的 Widget 交互测试。
- 未跟踪目录现在展开显示其中的文件；Changes 支持 Command 多选及批量暂存/取消暂存。
- 新增文件右键菜单中的路径复制、Finder 定位和未跟踪文件删除（确认后执行，已跟踪改动受保护）。
