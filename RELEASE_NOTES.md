# Release Notes

## Unreleased

- 工作区移除了重复显示仓库名的 Flutter 标签条；主题设置已移至唯一的 Git 操作栏末端，并在无仓库、加载或错误状态下保留在右上角；多仓库切换仍由独立窗口及 macOS 合并窗口的原生标签完成。
- 桌面界面现改用项目内 Flutter Material 主题，不再依赖 `yeknom_ui_kit`。深色模式采用石墨色
  分层表面、蓝色强调色、低圆角控件和紧凑系统字体；浅色模式保持相同的信息密度。主题菜单
  仅保留系统、浅色和深色模式；已有偏好中的颜色预设会安全忽略并在下次保存时清理。
- 打开含历史的仓库时，提交详情和提交改动区域现会默认显示最新提交；若该提交有可预览文件，会自动显示首个文件的差异。空仓库及无文件改动的提交仍显示对应空状态。
- 提交历史的“日期”表头与每条相对日期现均左对齐，列宽调整后仍保持一致。
- 统一未交付操作的可见状态：菜单、按钮和子菜单等用户可触发入口现在直接在标签中显示“（待实现）”，不会只以禁用态或点击后的提示表达；正式交付时会与占位提示一并移除该标注。
- 工作区文件右键菜单及 macOS“动作”菜单的“停止追踪”现对普通已追踪改动和已暂存但未提交的新增文件开放；确认后从 Git 索引移除所选路径但保留本地文件。已有提交的文件会刷新为待提交删除，已暂存新增文件则恢复为未追踪；对话框会说明提交前置条件以及未写入 `.gitignore` 时可能再次被添加的限制。
- 停止追踪后的文件列表遵循 Git 原始状态：已有提交的文件会同时显示为已暂存的删除和同路径未暂存的未跟踪文件，不再创建额外的“已停止追踪”分组。
- macOS 原生“动作”菜单的“停止追踪”现已移除“待实现”占位：仅在当前前台工作区选中适用的已追踪或已暂存新增文件时启用，调用与文件右键菜单相同的确认、Git 状态二次校验和索引移除流程，不会操作后台仓库。
- macOS“动作”菜单的“停止追踪”现也接受历史提交详情中选中的文件路径；执行前会验证它仍在当前工作区索引中且本地文件存在，避免历史选择误作用于已删除或已变更的路径。
- 修复“停止追踪”目标与界面高亮不一致的问题：历史提交文件优先作为动作目标，确认框会列出实际路径；同一文件同时存在暂存和未暂存修改时，确认后仍会仅移除索引项并保留本地最新内容。
- 已暂存已跟踪文件的右键菜单新增“重置…”：经确认后将选中路径的索引和工作区一起恢复到 HEAD，并明确提示会丢弃其暂存及未暂存内容。
- 重置文件会在确认后重新验证目标仍有已暂存改动；若外部操作已取消暂存或改变状态，则拒绝覆盖新的工作区内容并要求刷新后重试。
- 历史提交的文件列表新增 Sourcetree 风格右键菜单结构；所有尚未交付的项目都会直接标注“（待实现）”，“重置到提交…（待实现）”暂保持禁用，其余入口均为无副作用的待实现提示。
- 已存在合并工作区标签组时，从仓库管理页新打开的仓库会在 Git 验证成功后自动加入该组，并成为当前显示的标签。
- 仓库管理页的仓库条目现在需要双击才会打开或激活对应工作区窗口；单击不会再意外打开仓库。
- 仓库顶部 Git 操作栏现在完整显示可用操作，不再横向滚动；所有操作改为图标在上、标签在下的紧凑按钮，便于在同一行快速识别和使用。
- macOS 合并工作区窗口后，标题栏下方现在只显示一行 29pt 高的矩形标签：所有仓库按数量等分整行宽度，使用居中文字、直角选中态和 1pt 分隔线；过长名称显示省略号并保留完整悬停提示。应用不再同时创建系统胶囊标签条，切换时仍保留每个真实窗口及其独立 Flutter Engine 生命周期。
- 修复矩形标签按钮只按文字固有高度贴在标签条底部、导致标题栏与标签之间看似存在空隙的问题；按钮现在铺满 29pt 标签条并直接衔接标题栏。
- macOS 矩形工作区标签右侧新增关闭按钮；关闭任意标签只清理对应仓库窗口及其 Flutter Engine，关闭当前标签后自动切换到相邻工作区。
- 修复点击矩形工作区标签切换仓库时出现窗口缩放动画的问题；合并窗口现在关闭系统显隐动画，并原位同步标签标题与选中态，不再重复重建标签和调整 Flutter 内容区。空工作区随后绑定仓库时，标签文字、悬停提示和关闭按钮辅助说明也会立即更新。
- macOS 合并工作区标签现在支持拖动排序；拖动期间显示插入位置，松手后同步更新标签切换与关闭顺序，并保存为下次启动的合并标签恢复顺序。
- macOS 现在会在下次启动时自动恢复上次退出前仍打开的仓库工作区；若上次已选择“合并所有窗口”，恢复后的工作区仍会收拢为同一矩形标签组。手动关闭的窗口不会恢复，已移动、删除、不可读或不再能由 Git 验证的路径会自动清除。恢复状态仅保存本地路径和合并状态，不保存 Git 凭据、工作区内容或运行中的操作。
- 修复启动恢复合并标签组后，多个工作区残留的延迟置前任务互相抢占焦点、导致标签持续来回切换的问题；合并与标签切换现在会取消旧任务并只激活目标窗口一次。
- 修复恢复合并标签组时过早隐藏尚未完成初始化的工作区，导致重启后只剩一个可用标签的问题；现在会等待本批次全部仓库验证完成，再一次性建立矩形标签组。
- 恢复合并标签组现在只处理上次保存的窗口批次，不会把启动期间新开的独立工作区意外加入；等待 Git 验证超过 30 秒的恢复窗口会取消并关闭，不再永久阻塞窗口合并和状态保存。
- 修复 `flutter run -d macos` 的运行时 Dock 视图在图标资源已有透明安全边距后再次缩进 11% 的问题；Debug 启动的蓝色图标底板现在与相邻应用保持相近的视觉尺寸。
- 调整应用与 Dock 图标中央 Git 标记的比例，使主节点、主干和侧分支在 Dock 小尺寸下与参考图标保持相近的视觉占比；连接处继续使用连续无缝的白色几何体。
- 更新 macOS 应用与 Dock 图标：采用原创亮蓝色圆角底板与白色树形提交标记，主提交、短主干和侧分支在小尺寸下保持清晰，
  并使用真实透明外圈控制 Dock 光学尺寸；不包含 Sourcetree 商标或专有素材。
- 扩充并中文化 macOS 原生“窗口”菜单：最小化、缩放、仓库浏览器、合并窗口和系统窗口列表保持可用；窗口平铺、显示器、窗口组、标签页和远端托管仓库入口标为“待实现”，在首页或工作区点击只显示原生提示，不会改变窗口状态。

- 扩充 macOS 原生“动作”菜单，按 Sourcetree 的菜单层级保留打开、外部工具、暂存/重置、冲突恢复、审查与文件操作入口；创建与应用补丁保持可用，其他项明确显示“待实现”并只提示，不会执行 Git 操作。

- 修复交互式变基向下拖动提交时的排序偏移；`reword` 在内置提交信息编辑器完成前不再显示，避免无提示保留原提交消息。
- 遴选或回滚因冲突暂停后，工具栏会持续显示对应的继续与中止入口；仓库详情的总提交数、日期与作者表统一按全部引用统计，避免分叉仓库中数据不一致。

- macOS 原生菜单栏新增“仓库”菜单。“仓库详情…”以可调整大小的窗口展示当前仓库的真实 Git 统计、作者提交占比、LFS 提示和本地磁盘占用；空仓库安全显示。其余仓库入口保留并标注“待实现”，不会执行 Git 操作；流程入口只保留 Git Flow，不展示不在支持范围内的 hg flow。
- 修复补丁相关弹框在窗口小于初始尺寸时越界或拖动边缘/角落可能异常的问题；补丁导出不再受主历史搜索过滤影响，空列表提示可关闭。补丁检查或应用失败不再把整个工作区错误化。

- 本地分支右键菜单现在与 Sourcetree 的交互保持一致：普通未提交改动不再禁用重命名和删除；重命名弹窗默认全选旧名称，删除弹窗显示目标分支并提供“强制删除”和“删除远程分支”选项。默认安全删除仍由 Git 拒绝未合并分支，强制及远端删除必须明确勾选，冲突或仓库操作进行中仍不可执行。
- 提交 Graph 改为 Sourcetree 式连续活动车道：未变化的路径保持垂直，仅在分叉插入或路径结束时让右侧活动车道以 2px 短斜线同步补位，不再无条件逐行重排，也不会保留贯穿长历史的空洞和长横线；颜色顺序调整为蓝、橙、红、绿、紫、青等参照层级。
- 提交 Graph 现在最多绘制 8 条物理车道；第九条及更右侧的逻辑路径会折叠进第八条溢出车道，复杂仓库的提交节点不再被裁切隐藏，也不会越界覆盖提交描述，待活动车道收缩后会恢复独立显示。
- 提交历史现在支持分页加载：首次显示 100 条，并固定本次读取的本地分支与 HEAD 对象 ID；滚动到列表末尾会基于同一引用快照读取更早的提交，外部 Git 客户端在分页间移动引用不会造成重复或漏页。当前筛选范围没有匹配项时仍可继续加载；读取失败不会丢失已加载历史，并可在末尾重试。
- 修复在选择首屏外的分支或标签后继续加载历史会打乱提交时间顺序和 Graph 拓扑的问题；分页现在只追加规范的 Git 历史，不受临时引用预览影响。
- 仓库管理页右侧新增当前分支和未提交文件数状态徽标；信息直接来自 Git 状态，游离 HEAD 与尚未首次提交的仓库会明确标注。工作区完成 Git 状态变化后会同步刷新对应条目。
- 仓库管理页现在会使用仓库根目录中可读取的 `icon.png` 作为圆形仓库图标；图片缺失、不可读或无效时继续显示默认 Git 图标。
- macOS“窗口”菜单新增“合并所有窗口”。打开两个或更多仓库工作区后，可将它们收拢为标题栏下方的单行矩形标签；标签显示仓库名，首页不参与合并，关闭当前标签只释放该仓库自己的 Git 与 Flutter 资源。
- 仓库管理页现在支持从 Finder 拖入一个或多个目录添加仓库：松开前显示接收提示，应用会识别并保存实际 Git 根目录，跳过重复、不可读取和非仓库目录。仓库仍按父目录分组，可在同组内拖动手柄调整持久化显示顺序；筛选时排序暂不可用，避免误移动未显示的条目。
- 修复仓库管理页“筛选仓库”输入框的文字和搜索图标未垂直居中的问题。
- 修复从 Dock 重新激活应用时总是跳回仓库管理页的问题；现在恢复最后位于前台的首页或仓库工作区，已关闭窗口则按工作区最近使用顺序回退。
- 修复提交历史和详情中标签沿用分支图标的问题；标签现在使用独立 tag 图标和橙色样式。
- 修复创建非法格式标签时误报“读取仓库时发生未知错误”的问题；现在会明确提示 Git 标签名称规则并保留技术错误详情。
- 历史提交右键菜单新增推送、变基（含交互式 todo 编辑）、soft/mixed/hard 重置、提交回滚、补丁导出和遴选。交互式 todo 支持拖动排序及 pick、edit、squash、fixup、drop；遴选、回滚和 edit/变基冲突均可继续或中止。变基、回滚和遴选会在工作区不干净或 Git 操作进行中时拒绝执行；hard 重置必须额外确认其丢弃已跟踪未提交改动的影响。合并提交的回滚和遴选会要求选择主线父提交，补丁导出采用原子替换避免写入失败破坏已有补丁文件。
- macOS 菜单栏新增“动作”→“创建补丁…”与“应用补丁…”。创建补丁窗口复用仓库历史页的提交表格、Graph、引用标签、文件列表、Diff 和提交详情，可多选历史提交、按从旧到新的顺序合并导出，或在所选目录中生成独立文件；现有目标文件不会被覆盖。应用补丁可试运行、选择基础路径，并明确区分 Git 默认 `-p1` 与显式 strip 0–3。试运行成功后不再让工作区停留在加载状态。菜单只路由当前前台仓库窗口，不会误操作后台最近使用仓库；两个窗口支持拖拽四边或四角调整大小，内容与圆角表面同步移动并保持在可见边界内。
- 修复附注标签留空说明时无法安全创建的问题；标签创建失败会显示 Git 的实际原因，若本地创建成功但远端推送失败，会保留本地标签并明确说明部分成功结果。
- 修复历史“合并…”忽略被选提交的问题；现在会确认并合并该提交。非提交对象标签不再被错误地作为提交详情加载；远端标签删除成功但本地删除失败时会明确显示部分完成状态。
- 左侧“工作区”新增“历史”入口。“文件状态”仍提供完整的工作区文件与提交入口；在“历史”中点击 `Uncommitted changes` 则保留提交图和历史分栏，仅选中该虚拟行并在下方显示工作区改动，不再跳转到“文件状态”。
- 新增历史提交的 Tag 工作流：右键提交选择“标签…”后可创建轻量或带说明的附注标签，默认指向该提交，也可选择已加载的其他提交；可选只推送该标签到一个已配置远端。左侧“标签”现在显示 Git 读取到的本地标签并可定位目标提交；删除页支持删除本地标签，或在明确确认后同时删除指定远端同名标签。所有写操作结束后重新读取 Git 引用，远端推送失败不会掩盖已成功创建的本地标签。
- 左侧远端导航现在按已配置远端分组，展开后显示 Git 返回的 `HEAD` 符号引用和远端分支；右键远端名称可从该远端获取，并打开预选该远端的拉取、推送配置。“移除远端”会明确说明影响并确认，只移除本地 Git 配置和跟踪引用，绝不删除远端仓库或远端分支。
- 选中左侧“文件状态”后，引用导航右侧现在完整显示工作区文件、Diff 与提交信息入口，并隐藏常规历史和提交详情分栏；底部入口继续使用既有提交面板和 Git 写入流程。
- 新增完整的贮藏工作流：工具栏“贮藏”和左侧“已贮藏”入口（固定显示在远端之后）以紧凑创建弹框保存当前改动，可填写说明并保留暂存区；入口支持重复打开，且在没有可保存的已跟踪改动、存在冲突或 Git 操作进行中时明确禁用。左侧“已贮藏”会固定展开真实 Git 贮藏列表，每条记录显示分支和说明；点击会隐藏常规历史与详情分栏，并在引用导航右侧完整预览其中的文件和 Diff，右键菜单可列出、恢复并保留、恢复并弹出，以及明确确认后删除。默认不包含未跟踪文件。恢复仅在干净工作区执行，使用 `--index` 还原暂存区，并在冲突时保留贮藏和刷新冲突状态；未跟踪的嵌套 Git 仓库需在其自身仓库中单独处理。
- 抓取操作改为配置面板：默认抓取全部远端，可选清理失效跟踪分支及抓取所有标签；真实 Git 操作仍支持取消并在结束后刷新引用状态。
- 推送操作升级为多分支配置面板：可选择远端、勾选分支、手动输入或选择目标远程分支、设置跟踪关系及推送所有标签；推送始终不使用 force，并会在结束后刷新 Git 状态。
- 新增带动画的分支管理面板：支持从当前 HEAD 或指定历史提交创建本地分支、选择是否立即检出；删除页可多选本地/远端分支、显示类型列，并在明确确认后安全删除、强制删除未合并本地分支或请求删除远端分支。
- 修复复杂游离 HEAD 历史使用旧 Graph 算法导致节点、连线和共同祖先汇合不完整的问题；当前 HEAD 谱系现在稳定占用主蓝色车道，历史搜索也继续复用完整历史计算出的 Graph 颜色与拓扑。
- 修复嵌套合并历史中过早压缩车道、遗漏分支节点和连接线的问题；未变化的并行分支保持垂直，路径集合变化时仅同步移动受影响位置右侧的车道，节点、连线、提交标签和左侧图标统一使用实际车道颜色。
- 修复 detached HEAD 下无法切回本地分支的问题：普通未提交或未跟踪改动现在会随安全的 `git switch` 保留；存在未解决冲突时仍会禁用切换，Git 拒绝覆盖风险时保留原状态。
- 历史提交列表移除行间分割线，并将每行高度从 20px 微调至 24px，让提交图、引用和提交说明更易于连续扫读。
- 恢复历史列表的图表、描述、提交、作者、日期表头；表头与提交行高度独立，仍保持 24px 行高和无行间分割线。
- 修复游离 HEAD 位于分支共同基点时的 Graph 车道分配：最左工作区车道现在连续贯穿到 HEAD 基点，`main` 和 `codex/conflict-demo` 仅从各自提交开始使用右侧独立车道并回连基点，不再出现断线或提前绘制的侧分支。
- 蓝色主分支与红色侧分支现在保持各自车道直到橙色共同父节点所在行，再于父节点处汇入，不再在各自末端提交行提前向左转向。
- 提交图逐车道核对上一行的真实连接，蓝色和红色分支首节点上方不再绘制悬空的多余竖线，同时保留通往共同父节点的连续线段。
- 主分支、其他本地分支和 HEAD/远端标签现在分别复用蓝、红、橙 Graph 色；主分支领先 upstream 时在提交行显示“超前 N 个版本”。
- 历史引用标签现在会在游离 HEAD 时继续把可推送的主本地分支（如 `main`）显示为蓝色，并用红色区分其他本地分支、橙色区分远端与 HEAD；单个长分支名会优先完整显示，`codex/conflict-demo` 不再被固定 100px 宽度截断。
- 修复移除历史范围工具条后列表被错误锁定为“当前分支”的回归；无工具条模式现在始终展示 Git 已加载的全部分支提交与完整拓扑。
- 历史视图改为 Sourcetree 风格的紧凑提交列表：移除常驻显示工具条，保留列表表头，提交图与背景随浅色/深色主题切换；列宽拖拽仍可在历史内容中使用。
- 提交引用标签现在按当前本地分支、其他本地分支、远端分支和游离 HEAD 使用不同的图标与边框，便于快速识别引用类型。
- 左侧“分支”导航中的斜杠分隔分支现在会按可展开目录显示，例如 `codex/conflict-demo` 会显示为 `codex` 下的 `conflict-demo`；选择、切换与右键操作保持不变。
- 推送工具栏现在显示当前分支领先 upstream 的提交数；有远端推送目标时保持可点击，没有领先提交时执行无改动检查；游离 HEAD 时可使用已配置远端的本地分支完成推送。
- 修复游离 HEAD 推送目标在界面和执行层选择不一致、异常核验比较错误 HEAD，以及无工作区改动时预留灰色车道悬空的问题；附着 HEAD、多个同源侧分支、窄窗口长标签和提交详情引用颜色均补齐边界处理。
- 新增 Changes 面板的文件列表宽度调整，以及已暂存/未暂存文件区域高度调整。
- 在窗口空间不足时自动按最小边界收缩列和面板，并补充对应的 Widget 交互测试。
- 未跟踪目录现在展开显示其中的文件；Changes 支持 Command 多选及批量暂存/取消暂存。
- 新增文件右键菜单中的路径复制、Finder 定位和未跟踪文件删除（确认后执行，已跟踪改动受保护）。
